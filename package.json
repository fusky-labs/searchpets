{
  "name": "searchpets",
  "author": {
    "name": "openfurs"
  },
  "private": true,
  "workspaces": [
    "searchpets-v2-next"
  ],
  "scripts": {
    "reset:next": "rm -rf app/node_modules app/yarn.lock app/package-lock.json && yarn --cwd ./app install",
    "dev": "concurrently -n \" next \",python -c bgBlack,bgBlue.yellow -k \"npm run dev:next\" \"npm run server:dev\"",
    "build": "concurrently -n \" next \",python -c bgBlack,bgBlue.yellow -k \"yarn --cwd ./app build\" \"yarn server:start\"",
    "start": "concurrently -n \" next \",python -c bgBlack,bgBlue.yellow -k \"npm run start:next\" \"npm run server:start\"",
    "server:dev": "cd server && python app.py",
    "server:start": "cd server && python app.py --build",
    "dev:next": "yarn --cwd ./app dev",
    "start:next": "yarn --cwd ./app start -p 80",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OpenFurs/searchpets.git"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "husky": "^8.0.0"
  },
  "license": "GPL-2.0"
}

{
  "name": "searchpets",
  "description": "A search engine to find your favorite Housepets comic!",
  "scripts": {
    "dev": "concurrently -n next,python \"npm run dev:next\" \"npm run server\"",
    "start": "concurrently -n next,python \"npm run start:next\" \"npm run server\"",
    "generate": "cd server && python3 gen.py",
    "server": "cd server && python3 app.py",
    "dev:next": "yarn --cwd ./app dev",
    "build": "yarn --cwd ./app lint && yarn --cwd ./app build",
    "start:next": "yarn --cwd ./app start -p 80"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OpenFurs/searchpets.git"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "sharp": "^0.30.5"
  }
}
